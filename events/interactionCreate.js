// Routes commands, autocomplete, components and modals with global error handling.
const embedCommand = require('../commands/embed');
module.exports = { name: 'interactionCreate', async execute(interaction) { try { if (interaction.isAutocomplete()) return interaction.client.commands.get(interaction.commandName)?.autocomplete?.(interaction); if (interaction.isChatInputCommand()) return interaction.client.commands.get(interaction.commandName)?.execute(interaction); if (interaction.isButton() || interaction.isStringSelectMenu()) { if (interaction.customId.startsWith('embed:')) return embedCommand.handleComponent(interaction); } if (interaction.isModalSubmit()) { if (interaction.customId.startsWith('embed:')) return embedCommand.handleModal(interaction); } } catch (error) { console.error('Interaction error:', error); const payload = { content: `Something went wrong: ${error.message}`, ephemeral: true, components: [] }; if (interaction.deferred || interaction.replied) await interaction.followUp(payload).catch(console.error); else await interaction.reply(payload).catch(console.error); } } };
