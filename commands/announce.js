// /announce embed announcement command.
const { SlashCommandBuilder, PermissionFlagsBits, EmbedBuilder } = require('discord.js');
module.exports = { data: new SlashCommandBuilder().setName('announce').setDescription('Send an embed announcement.').setDefaultMemberPermissions(PermissionFlagsBits.ManageMessages).addStringOption((o) => o.setName('title').setDescription('Announcement title.').setRequired(true)).addStringOption((o) => o.setName('message').setDescription('Announcement body.').setRequired(true)).addStringOption((o) => o.setName('color').setDescription('Hex color.').setAutocomplete(true)), async autocomplete(i) { await i.respond(['#5865F2','#57F287','#FEE75C','#ED4245','#EB459E'].map((c)=>({name:c,value:c}))); }, async execute(interaction) { const embed = new EmbedBuilder().setTitle(interaction.options.getString('title')).setDescription(interaction.options.getString('message')).setColor(interaction.options.getString('color') || '#5865F2').setTimestamp(); await interaction.reply({ embeds: [embed] }); } };
